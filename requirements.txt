# モノシリ - 依存ライブラリ
# pip install -r requirements.txt

# ─── UI ───────────────────────────────────────────────────────
streamlit>=1.40.0

# ─── ベクトルDB ───────────────────────────────────────────────
chromadb>=0.5.0

# ─── Embeddingモデル（常時ローカル）──────────────────────────
sentence-transformers>=3.0.0

# ─── ローカルLLM推論 ──────────────────────────────────────────
# ADR-001: Ollama HTTP API に移行済み（別途インストール: https://ollama.com）
# llama-cpp-python は不要

# ─── ファイルテキスト抽出 ──────────────────────────────────────
PyMuPDF>=1.24.0          # PDF (fallback)
pdfplumber>=0.11.0       # PDF テーブル構造保持抽出（IMPROVE-001）
python-docx>=1.1.0       # Word (.docx)
python-pptx>=1.0.0       # PowerPoint (.pptx)
openpyxl>=3.1.0          # Excel (.xlsx)

# ─── テキスト分割 ─────────────────────────────────────────────
langchain-text-splitters>=0.3.0

# ─── モデルダウンロード ───────────────────────────────────────
huggingface_hub>=0.25.0

# ─── システム情報 ─────────────────────────────────────────────
psutil>=5.9.0
