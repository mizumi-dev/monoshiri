# チャット精度テスト 問題分析と修正計画

**テスト実施**: 2026-04-18 11:45  
**対象**: モノシリ Qt版 ChatWidget（本番データ28,692チャンク使用）  
**結果**: 9問中 3問合格（33.3%）

---

## 1. テスト結果サマリ

| カテゴリ | 質問数 | 合格 | 状況 |
|---------|-------|------|------|
| 資料関連 | 3 | 3 (100%) | ✅ 正確な数値回答 |
| 資料外 | 3 | 0 (0%) | ❌ 関連資料があると回答してしまう |
| 全く無関係 | 3 | 0 (0%) | ⚠ LLMは正しく「見つからない」と返すが、found=Trueとなる |

---

## 2. 発見された問題

### 問題A: 資料外の質問に「関連するがノイズ的な」資料から回答している

**例**: 「量子コンピュータの現在の技術動向は？」
- **ヒットしたエビデンス**: NTT統合報告書2025（類似度0.842）
- **LLM回答**: 「光量子コンピュータを開発しており...」と回答
- **原因**: NTT報告書に量子コンピュータ関連の記述が実在する。**これはハルシネーションではなく、本来の財務・運営資料で言及されている**
- **評価**: 技術的には正しいが、「資料外」分類とは矛盾する

**例**: 「人工知能の倫理規定について教えて」
- **ヒットしたエビデンス**: Microsoft/Meta 10-K（類似度0.844）
- **LLM回答**: 各社のリスク開示から倫理的リスクを抽出
- **評価**: 同上。財務報告書にAIリスクの開示があるため回答可能になっている

### 問題B: 無関係な質問でも `found=True` になる（ADR-002不完全）

**例**: 「今日の天気は？」
- **ヒットしたエビデンス**: 三菱重工中間報告（類似度0.785）など
- **LLM回答**: 「文書に今日の天気が記載されていないため、答えられません」（正しい動作）
- **問題**: エビデンスは8件ヒットしており `found=True` だが、LLMは実質「見つからない」と応答
- **根本原因**:
  - `DEFAULT_SIMILARITY = 0.40` は緩すぎる。コサイン類似度0.77でも閾値通過
  - `core/rag.py` の `found` 判定は `len(similar_items) > 0` のみ（LLM回答の内容を見ていない）

### 問題C: 類似度スコアの分布が日本語資料で高止まり

- multilingual-e5-large は日本語の汎用的な単語（「企業」「事業」等）で高類似度を返しやすい
- 無関係な質問でも 0.77 前後の類似度が出る
- 0.40 という低閾値では事実上ほぼ全質問にエビデンスが付く

---

## 3. コード上の問題箇所

### 3-1. `core/config.py` — 類似度閾値

```@c:\Users\motok\開発・IT・API\モノシリ\core\config.py:1-1
DEFAULT_SIMILARITY = 0.40  # ADR-002準拠だが、実運用で低すぎる
```

ADR-002で0.40が定められているが、multilingual-e5-largeの日本語分布では**最低ラインが0.70〜0.75**であり、0.40では「関連度なし」を弾けない。

### 3-2. `core/rag.py` — found判定ロジック

現在の `answer_question` / `stream_question` では:
- `found = len(similar_items) > 0`
- LLM回答の内容は `found` に反映されない

### 3-3. 日本語テキスト分布の影響

Embedding時にE5は「passage: 」プレフィックスを期待するが、運用内で一貫していれば相対的にはOK。**絶対閾値ではなくZスコアや相対的カットオフが必要**。

---

## 4. 修正計画

### 優先度 高

#### 修正1: 類似度閾値の引き上げ（`core/config.py`）
```python
# 変更前
DEFAULT_SIMILARITY = 0.40
# 変更後（推奨）
DEFAULT_SIMILARITY = 0.72
```
- 根拠: 本テストで資料関連3問の最低類似度 = 0.864、無関係3問の最高類似度 = 0.785
- 中間値の 0.80 付近が理想だが、保守的に 0.72 から開始して微調整
- **要ADR-002更新**: 根拠の再検証と値の改定を記録

#### 修正2: found判定にLLM回答パターンを反映（`core/rag.py`）
`answer_question` の戻り値で、LLM回答に以下のパターンが含まれる場合 `found=False` とする:
- 「見つかりません」「見つかりませんでした」
- 「記載されていません」「記載はありません」「情報はありません」
- 「答えられません」「回答できません」

```python
NO_ANSWER_PATTERNS = [
    "見つかりません", "見つかりませんでした",
    "記載されていません", "記載はありません",
    "情報はありません", "情報が含まれていません",
    "答えられません", "回答できません",
]

def _is_no_answer(answer: str) -> bool:
    return any(p in answer for p in NO_ANSWER_PATTERNS)

# answer_question() / stream_question() 末尾で
if _is_no_answer(final_answer):
    return {"answer": final_answer, "found": False, "evidence": []}
```

### 優先度 中

#### 修正3: 相対類似度カットオフ
- Top Kの最高類似度との差が一定値（例: 0.05）以上離れたエビデンスを除外
- ノイズ的なエビデンスを抑制

#### 修正4: UIエビデンス表示の改善（`ui_qt/chat_widget.py`）
- LLMが「見つからない」と返した場合、エビデンスを表示しない
- ユーザーの誤解を防ぐ

### 優先度 低

#### 修正5: カテゴリ別閾値設定
- 設定UIで「厳格」「標準」「緩和」の3段階を提供
- `similarity_threshold_slider` の既存スライダーと連動

---

## 5. 修正後の期待される結果

| カテゴリ | 合格率（修正前） | 合格率（修正後予測） |
|---------|---------------|-------------------|
| 資料関連 | 100% | 100%維持 |
| 資料外 | 0% | 67〜100% |
| 全く無関係 | 0% | 100%（LLM回答パターンで弾く） |

---

## 6. 次のステップ

1. 修正1（閾値引き上げ）と修正2（LLMパターン検出）を実装
2. 同じテストスクリプトで再評価
3. 資料関連の合格率が落ちていないことを確認
4. ADR-002の更新

---

## 7. 補足: 質問カテゴリの意味合いについて

「資料外」カテゴリは微妙な問題を含む:
- 量子コンピュータ・AI倫理は**財務報告書の開示事項に含まれうる**
- つまり「本当に資料外」とは言えない
- よりクリーンな「資料外」質問例:
  - 「この資料で登場する料理のレシピを教えて」
  - 「文書内の登場人物の恋愛関係は？」

再評価時にはこうしたより純粋な質問を追加するとより正確な判定ができる。
